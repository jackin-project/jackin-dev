interface:
  display_name: "Jackin refresh pr"
  short_description: "Reconciles an open jackin❯ pull request's title and body against the current diff — regenerates the Verify-locally block selection and rewrites prose that has drifted."
  default_prompt: "Use $jackin-refresh-pr for this jackin repository task."
policy:
  allow_implicit_invocation: false
