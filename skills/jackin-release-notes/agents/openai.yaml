interface:
  display_name: "Jackin release notes"
  short_description: "Classifies every PR merged since the last tag into Keep a Changelog categories and writes the [Unreleased] section of CHANGELOG.md."
  default_prompt: "Use $jackin-release-notes for this jackin repository task."
policy:
  allow_implicit_invocation: false
